/**
 * Shared hooks for the website.
 * Import from '@site/src/hooks' for consistent patterns across components.
 */

export { useLocale, useTranslations } from './useLocale';
export { useApi } from './useApi';
